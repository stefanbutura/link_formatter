{% if url_type == "image" %}
    <img src="{{ uri }}" height="420" width="420">
{% elseif url_type == "youtube" %}
    <iframe width="420" height="315"
            src={{ uri }}>
    </iframe>
{% endif %}